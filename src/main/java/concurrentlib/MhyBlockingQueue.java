package concurrentlib;

/**
 * A {@Link util.MhyQueue} that additionally supports operations
 * that wait the queue become non-empty when retriving an
 * element, and wait for space to become available in the queue
 * when storing an element.
 *
 * @since   20210418
 * @author  miaohongyuan
 * @param <E> the type of element held in this collection
 */
public interface MhyBlockingQueue<E>  {
    
}
